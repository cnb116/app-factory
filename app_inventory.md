| 번호 | 앱 이름 | 배포 URL | 핵심 기능 | 결제 상태 | 마케팅 자재 | 가동 상태 | 폴더 경로 | 수정 이력 |
|---|---|---|---|---|---|---|---|---|
| 01 | 텍스트 정리기 | https://app-factory-text-cleaner.vercel.app | 글자수/단어수 계산, 공백 정리, 정리된 글 복사(클립보드), AI 맞춤법 교정(PremiumCapsule로 3,900원 안내 표시, 하루 3회 무료 후 초과 시 결제 준비 중 알림, localStorage 기반 날짜별 자동 초기화), 교정 결과 변경 부분 노란 하이라이트 표시 + 숫자 3자리 이상 포함 시 재확인 경고 | 하루 3회 무료 제한 재도입(2026-09-13) — 실제 토스 결제 연동은 아직, 4회째부터 "결제 준비 중입니다" 안내만 | [marketing_assets.md](01-text-cleaner/marketing_assets.md) | 🟢 정상 — GCP 프로젝트는 정지 상태 아님(키 자체 정상), 다만 코드가 쓰던 구모델(`gemini-2.5-flash`)이 지원 종료되어 실제로 502 장애 중이던 것을 발견·수정(2026-09-14, `gemini-3.6-flash`로 교체 후 정상 복구 확인) | app-factory/01-text-cleaner | 2026-08-30 최초 생성<br>2026-08-30 정리 버튼 동작 재검증(로컬 프로덕션 빌드로 정상 작동 확인, 코드 결함 없음) 및 재배포 진행<br>2026-08-30 Vercel Authentication 해제, 공개 URL로 실동작·에러 로그 확인 완료<br>2026-08-30 맞춤법 교정 버튼 UI 개선 (실연동 전 단계)<br>2026-08-30 유료 버튼 문구를 "평생 소장" 개념으로 수정<br>2026-08-30 유료 버튼 라벨 중복 제거 (위 라벨만 유지)<br>2026-08-30 Gemini API 키 환경변수(.env.local) 및 유틸 함수 준비 (실연동 전 단계, 미배포)<br>2026-08-30 Gemini API 실연동(하루 3회 무료, localStorage 기반, 초과 시 가짜 알림). 로컬 실제 키로 정상 교정 확인. Vercel 환경변수 미등록으로 배포본은 아직 미작동 — 등록 필요<br>2026-08-30 Vercel GEMINI_API_KEY 등록 및 재배포 완료. 배포본에서 3회 실작동+4번째 가짜 알림 전환 실사용 검증 완료<br>2026-08-30 앱 아이콘(icon.svg, apple-icon.png 180x180) 적용 및 배포본 반영 완료<br>2026-08-30 버튼/텍스트 크기 균형 조정 (버튼 축소, 본문·라벨 확대), 모바일 화면 실동작 검증 완료<br>2026-08-30 정리된 글 복사 버튼 추가(클립보드 복사, "복사됨! ✓" 피드백, 폴백 처리), 배포본 실동작 검증 완료<br>2026-08-30 무료 정리 버튼 위/아래 "무료" 라벨 중복 제거, 통계 카드 3개 모바일 뷰포트에서도 가로 3열 배치되도록 수정(sm: 접두사 제거), 배포본 390px 모바일 검증 완료<br>2026-08-31 출하 프로세스 실행: 마케팅 자재 5종(블로그/숏폼 대본/숏폼 제목·해시태그/지식인·게릴라 멘트/Kmong 원고) 작성 완료, 장부 병합 양식(마케팅자재·가동상태 컬럼) 갱신<br>2026-08-31 문단 내 줄바꿈 합치기 기능 추가(빈 줄 구분 문단은 유지, 문장 중간 줄바꿈만 공백으로 병합), 무료/유료 버튼에 FREE(초록)/유료(검정) 배지 추가, 배포본 테스트 케이스·모바일 배지·기존 기능 회귀 검증 완료<br>2026-08-31 중복 문구 정리: "하루 3회 무료 체험..." 삭제(배지로 대체), "블로그 원고..." 설명을 무료 정리 버튼 쪽으로 이동, 유료 버튼 위엔 "오늘 남은 무료 횟수" 한 줄만 유지. 배포본 실동작·에러 로그 검증 완료<br>2026-09-04 프리미엄 UI 절제된 스타일로 변경(코드 작성만, 미배포): "FREE" 초록 뱃지 완전 삭제(정리 버튼은 별도 표시 없이 자연스럽게). "유료" 검정 뱃지 + 큰 노란 배경 버튼 → 흰 배경·얇은 회색 테두리(border-zinc-300)의 캡슐형 버튼으로 교체, 좌측에 🔒 아이콘 + "프리미엄 확장팩 (AI 맞춤법 교정) — 3,900원" 한 줄(font-weight 400, 굵지 않음)로 변경, 기존 결제 준비 중 알림 로직은 그대로 유지. "오늘 남은 무료 횟수"→"오늘 남은 횟수"로 "무료" 단어 제거. 기존 화면 순서·구조는 그대로 두고 겉모습(뱃지/버튼 스타일)만 교체. 로컬에서 getComputedStyle로 border/backgroundColor/fontWeight/borderRadius 값 직접 확인 및 "FREE"·"무료" 문자열 전무 확인, 콘솔 에러 없음. 02호기(마케팅 페이스메이커) 프리미엄 기능 UI의 기본 스타일 기준으로 채택 예정<br>2026-09-04 실제 "마음 권력" 앱 참고 스크린샷 확인 후 프리미엄 캡슐 색감 보정: 회색 테두리·회색 텍스트(border-zinc-300/text-zinc-600) → 참고 이미지와 동일하게 배경은 완전 투명(bg-transparent), 테두리·텍스트는 골드 계열(border-amber-400/text-amber-700)로 교체. 로컬에서 getComputedStyle로 backgroundColor(투명)·borderColor·textColor 골드 톤 확인 완료<br>2026-09-05 프리미엄 UI 표준화(코드 작성만, 미배포): 재사용 가능한 `components/PremiumCapsule.tsx` 컴포넌트 신규 제작(description/price/subtext/onClick/isLoading props, onClick 없으면 비클릭 안내용 div로 렌더링). 기존 인라인 캡슐 버튼을 이 컴포넌트로 교체. "오늘 남은 무료 횟수" 카운트다운 및 `lib/usage.ts`(하루 3회 제한 로직) 완전 삭제 — AI 맞춤법 교정은 결제 로직이 실제로 붙기 전까지 무제한으로 동작(버튼 자체는 계속 "3,900원" 프리미엄으로 표기, 향후 결제 연동 시 이 자리에 게이팅만 추가하면 됨). 로컬에서 5회 연속 교정 실행해도 "결제 준비 중" 알림이 더 이상 뜨지 않음을 확인, "무료"/"FREE" 문자열 전무 확인, 콘솔 에러 없음. 저장소 루트에 `PREMIUM_UI_STANDARD.md` 신규 작성(향후 3~30호기 공통 표준 문서)<br>2026-09-05 프리미엄 UI 표준화 배포 및 검증: 로컬에서 5회 연속 AI 맞춤법 교정 실행(더 이상 안 막힘), 캡슐 상시 노출·"FREE"/"무료" 문자열 전무, 클릭 시 실제 교정 즉시 실행(결제 로직 미연동이라 현재는 무제한으로 작동 — 안내만 하는 게 아니라 기능이 실제로 동작함), 콘솔 에러 없음. 모바일 뷰포트(375x812)로도 동일 확인. Vercel CLI로 프로덕션 재배포 후 배포본에서도 캡슐 노출·5회 연속 교정·콘솔 에러 없음 재확인 완료<br>2026-09-05 변경사항 하이라이트 + 숫자 주의 안내 추가(코드 작성만, 미배포): `lib/diff.ts` 신규 작성(LCS 기반 단어 단위 diff, `diffWords`/`hasLongDigitRun`). AI 맞춤법 교정 후 원본 대비 바뀐 단어만 노란 배경(`<mark>`)으로 표시하는 읽기 전용 뷰를 추가, 결과 텍스트를 클릭하면 다시 일반 편집 가능한 textarea로 전환(간단한 클릭-투-에딧 방식, contentEditable 등 복잡한 구현 없이). 교정 결과에 숫자 3자리 이상 연속 패턴이 있으면 결과창 바로 위에 "⚠️ 숫자는 AI가 놓칠 수 있어요. 다시 한 번 확인해주세요." 안내 노출. "정리" 버튼 클릭 시에는 하이라이트/경고 모두 초기화. 새 화면·버튼 추가 없이 기존 텍스트 박스 자리만 활용. 로컬에서 (1) 오타+전화번호 포함 문장 교정 → 변경 부분만 정확히 하이라이트 + 경고 노출 (2) 숫자 없는 문장은 경고 미노출 (3) 결과 박스 클릭 시 편집 모드 복귀 (4) "정리" 클릭 시 하이라이트 해제 — 데스크톱·모바일(375x812) 둘 다 확인, 콘솔 에러 없음<br>2026-09-05 전체 플로우 재검증 및 배포: 로컬에서 처음부터 다시 (1)공백/줄바꿈 정리 (2)통계 카드 갱신 (3)AI 교정 후 하이라이트+숫자 경고 (4)결과 박스 클릭→편집 복귀 (5)"정리" 클릭 시 하이라이트 해제 (6)3회 연속 재교정에도 막히지 않음 전부 재확인, 콘솔 에러 없음. (클립보드 복사만 이 브라우저 자동화 환경의 "Document is not focused" 제약으로 테스트 불가 — 오늘 변경한 코드와 무관한 기존 기능이라 회귀 아님, 실제 사용자 클릭 환경에서는 기존처럼 정상 작동). Vercel CLI로 프로덕션 재배포 후 배포본에서도 정리·AI교정 하이라이트/경고·클릭 편집 복귀·3회 연속 재실행 전부 재확인 완료, 콘솔 에러 없음<br>2026-09-05 교정 범위 안전장치 추가(코드 작성만, 미배포): 실사용 테스트(청첩장 문자)에서 발견된 문제 2가지 개선. (1) `/api/correct-text`의 Gemini 프롬프트를 6개 규칙으로 명시화 — 맞춤법/띄어쓰기만 수정, 축약어·은어(예: "B4F")는 절대 풀어쓰지 않음, 원래 의미·어투 유지(의역 금지), 줄바꿈 구조 유지, "[Web발신]" 같은 통신사 광고성 발신 태그만 제거, 그 외 본문은 건드리지 않음. (2) `handleClean`(정리 버튼)에 숫자-하이픈 패턴(`/\d{2,}-\d/`) 예외 규칙 추가 — 계좌번호·전화번호처럼 해당 패턴이 있는 줄은 앞뒤 줄바꿈을 보존한 채 독립된 줄로 유지하고, 그 줄 자체의 공백만 정리(일반 문장 줄들끼리는 기존대로 한 줄로 병합). 로컬에서 실제 청첩장 문자 형태 예시("[Web발신]" + "B4F" + 계좌 3줄)로 검증: [Web발신] 제거됨, B4F 그대로 유지(하이라이트 안 됨), 계좌번호 줄 독립 유지, 숫자 경고 배지 정상 노출, 하이라이트도 실제 변경분(빈칸 등)만 정확히 표시. 숫자 없는 일반 문단은 기존처럼 한 줄로 병합되는 회귀 없음 확인. 데스크톱·모바일(375x812) 둘 다 확인, 콘솔 에러 없음. 배포는 진행하지 않음(전체 테스트 후 별도 배포 지시서 예정)<br>2026-09-05 교정 안전장치 반영 배포: 정리/통계/AI교정/하이라이트/숫자경고/PremiumCapsule/무제한 재실행까지 전체 기능을 로컬에서 실제 클릭(스크립트 클릭 아닌 실제 마우스 클릭+타이핑)으로 청첩장 문자 예시 기준 처음부터 재검증 — [Web발신] 제거, B4F 그대로 유지, 계좌번호 줄 독립 유지, 숫자 경고 노출, 3회 연속 재교정에도 안 막힘, 콘솔 에러 없음(데스크톱+모바일 375x812). Vercel CLI로 프로덕션 재배포 후 배포본에서도 동일 시나리오(실제 클릭 기준) 전부 재확인 완료. 클립보드 복사는 로컬·배포본 둘 다 `navigator.clipboard.writeText` 호출 시 `NotAllowedError: Write permission denied`로 확정 진단 — Claude Browser 미리보기 프레임 자체의 Permissions-Policy가 클립보드 쓰기를 차단하는 구조적 제약(포커스 문제가 아님, 앱 코드와 무관)이라 이 환경에서는 검증 불가. 실제 사용자 브라우저에서는 이전 세션에 실사용 검증된 대로 정상 작동 예상<br>2026-09-05 Vercel Web Analytics 연동: `@vercel/analytics` 패키지 설치, `app/layout.tsx`에 `<Analytics />`(from `@vercel/analytics/next`) 추가. 로컬 빌드 정상, 기존 UI·콘솔 에러 없음 확인 후 Vercel CLI로 프로덕션 배포 완료<br>2026-09-13 입력창 크기·에러 조사: 배포본 접속해서 실제 에러 확인 결과 `/api/correct-text`가 502 반환 → 원인은 Gemini API 키 자체가 `401 UNAUTHENTICATED / ACCOUNT_STATE_INVALID`("The bound service account is deleted or disabled")를 반환하는 것으로 확정 — 코드 버그가 아니라 Google Cloud 쪽에서 이 API 키에 연결된 서비스 계정이 비활성화된 외부 문제. 대장님이 Google AI Studio/Cloud Console에서 새 API 키를 발급하고 Vercel 환경변수(GEMINI_API_KEY)를 갱신해야 해결됨(코드로 고칠 수 없는 부분). 입력창 크기는 git 히스토리 전수 조사 결과 최초 커밋부터 지금까지 `h-64 sm:h-80`(모바일 256px/데스크톱 320px)로 단 한 번도 변경된 적 없음을 확인 — 요청하신 "최소 200px 이상" 기준을 이미 넘고 있어 실제 축소는 없었음(별도 수정 안 함). 원인 진단을 쉽게 하도록 `/api/correct-text`의 실패 분기에 `console.error` 로깅 추가(app 02와 동일 패턴, Vercel 로그로 실제 Gemini 에러 확인 가능해짐). 정리/통계/복사 버튼/캡슐/무료 문구 없음 등 나머지 기능은 로컬·배포본 모두 회귀 없이 정상 확인, 클라이언트 콘솔 에러 없음(502는 서버 응답이라 별개). 로깅 추가분만 커밋+push+Vercel 배포 완료<br>2026-09-13 API 키 재발급 후 재확인 — 여전히 장애: 대장님이 새 Gemini API 키로 교체(Production+Preview)하고 재배포하셨으나, 실제 재현 결과 502가 그대로 재현됨. `get_runtime_errors`와 실시간 Vercel 로그로 확인한 정확한 원인은 이전과 다름 — 이번엔 `403 PERMISSION_DENIED / CONSUMER_SUSPENDED`("Consumer 'api_key:...' has been suspended", consumer projects/992228513660): 새로 발급한 키 자체는 유효하지만, **그 키가 속한 Google Cloud 프로젝트가 정지된 상태**라 여전히 호출이 거부됨(이전의 "서비스 계정 비활성화"와는 다른 별개의 원인). 코드/배포 문제 아님 — Google Cloud Console에서 해당 프로젝트의 정지 사유(결제 계정 미설정·정책 위반 등) 확인 후 해제하거나, 완전히 다른 정상 프로젝트에서 새 키를 발급받아야 해결됨. 상태는 🟡 부분 장애로 유지(🟢로 갱신하지 않음 — 실제로 복구 안 됨). 참고로 "3회 무료 후 결제 알림" 로직은 2026-09-05 프리미엄 UI 표준화 때 완전히 제거되어 더 이상 존재하지 않음(현재는 무제한 무료 구조이며 재도입되지 않음) — 이번 지시서의 해당 항목은 현재 코드와 맞지 않는 전제였음을 확인<br>2026-09-13 결제 유도 로직(하루 3회 무료 제한) 재도입: 2026-09-05에 삭제했던 `lib/usage.ts`(날짜별 사용 횟수 localStorage 기록, 자정 기준 자동 초기화)를 삭제 전과 동일한 코드로 복원. `handlePremiumClick`에서 API 호출 전에 `getRemainingFreeUses()`로 잔여 횟수를 확인해 0이면 API 호출 없이 "결제 준비 중입니다" 알림만 표시, 3회 이하면 정상 호출 후 성공 시에만 `consumeFreeUse()`로 1회 차감(실패 시에는 차감 안 함). 버튼 위에 "오늘 남은 무료 횟수: N회" 문구 복원. 09-05 이후 도입된 UI(PremiumCapsule 골드 캡슐, FREE/유료 뱃지 제거, 하이라이트/숫자경고 등)는 전혀 건드리지 않음. 로컬에서 fetch를 모킹해 (1)3회 연속 성공 시 3→2→1→0으로 정상 소진 (2)4번째 클릭 시 API 호출 자체가 발생하지 않고 "결제 준비 중입니다" 알림만 뜨는 것 (3)localStorage의 날짜를 과거로 바꾸면 자동으로 3회로 초기화되는 것 전부 확인. 정리·복사·캡슐 스타일 등 다른 기능 회귀 없음, 콘솔 에러 없음<br>2026-09-14 GEMINI_API_KEY GCP 프로젝트 점검 및 조치 — 선제 점검 중 실제 장애 발견: 02호기가 겪은 "정지된 GCP 프로젝트" 문제가 01호기에도 있는지 확인하기 위해 `vercel env pull`로 Production 키를 가져와 Gemini API에 직접 테스트. 결과: 이 키가 속한 GCP 프로젝트는 정지 상태가 아니며(CONSUMER_SUSPENDED 아님) 키 자체는 정상 — 다만 `gemini-2.5-flash`로 직접 호출 시 `404 NOT_FOUND / "This model models/gemini-2.5-flash is no longer available to new users"` 응답, 반면 같은 키로 `gemini-3.6-flash`는 정상 200 성공. 즉 이 프로젝트는 정지된 게 아니라 코드가 쓰는 구모델 자체가 지원 종료된 것 — 배포된 프로덕션에 실제로 502 에러가 발생 중임을 라이브 curl로 확인(대장님이 인지 못하고 계셨을 가능성 있는 실제 장애). `app/api/correct-text/route.ts`의 모델명을 `gemini-3.6-flash`로 교체, 로컬 실제 API 호출로 정상 교정 확인 후 Vercel 프로덕션 재배포. 배포본에도 실제 요청으로 재현 검증(HTTP 200, 오타 교정 정상, 은어/축약어 보존 규칙도 정상 작동), 브라우저로 전체 플로우(입력→AI 교정→하루 무료 횟수 3→2 정상 차감)까지 재확인, 콘솔 에러 없음. 결론: 01호기는 02호기와 달리 GCP 프로젝트 정지 문제는 없었으나, 같은 근본 원인 계열(구모델 지원 종료)로 별도 장애가 있었고 이번에 해결함<br>2026-09-19 띄어쓰기 교정 기능 추가(코드 작성만, 미배포): 기존 "줄바꿈 정리"만 있던 무료 기본 기능 옆에 "띄어쓰기 교정"을 별도 토글로 추가(기본값 둘 다 켜짐). `lib/textClean.ts` 신규 — 기존 `page.tsx`에 인라인으로 있던 줄바꿈 정리 로직을 `cleanLineBreaks()`로 그대로 옮기고, 신규 `fixSpacing()`(스페이스 2칸 이상→1칸, 마침표/쉼표/물음표/느낌표 앞 공백 제거, 카톡 복사 특유의 "제 가"→"제가" 같은 한 글자 한글 음절 연쇄 붙이기)를 추가. 최초 구현에서 한 글자+공백을 오버랩 매칭하는 정규식(`(?:[가-힣][ \t]){1,}[가-힣]`)을 썼다가 실제 문장("이건 스페이스가 많은 문장입니다" 등 정상 문장)의 공백이 통째로 사라지는 심각한 버그를 자체 발견 — 원인은 정규식이 실제 단어의 마지막 한 글자를 다음 단어와 우연히 겹쳐 매칭하는 것. 공백 기준으로 토큰을 나눠 "한 글자 한글 토큰이 연속 2개 이상"인 구간만 합치는 방식(`joinSpacedHangulSyllables`)으로 재작성해 해결, Node로 단위 테스트(정상 문장 보존 + "제 가"/"정 상 적 인..." 등 깨진 패턴만 정확히 병합) 확인 후 반영. `page.tsx`에 체크박스 2개(줄바꿈 정리/띄어쓰기 교정, 기본 체크) 추가, `handleClean`이 두 토글 상태에 따라 `cleanText()` 호출하도록 교체. 정리 전/후 diff 하이라이트는 기존에 AI 교정 결과에만 쓰이던 `diffWords`+노란 `<mark>` 하이라이트 UI를 그대로 재사용해 무료 "정리" 버튼 결과에도 적용(별도 UI 신규 제작 없이 기존 컴포넌트 재사용). 로컬에서 실제 카톡 복사글 스타일 텍스트("안녕하세요 .   오늘   회의는" + "제 가 준비한   자료는..." + 계좌번호 줄)로 실행해 마침표 공백 제거·다중 공백 정리·"제가" 병합·계좌번호 줄 보존이 전부 정확히 적용되고 해당 부분만 노란 하이라이트로 표시됨을 스크린샷과 DOM 조회로 확인, 클릭 시 재편집 모드 복귀 정상, "띄어쓰기 교정" 체크 해제 시 마침표 공백·음절 병합이 정확히 미적용되는 것도 확인(토글 독립 동작 검증), 콘솔 에러 없음. 맞춤법 검사(Gemini AI 교정)·PremiumCapsule 관련 로직은 전혀 변경하지 않음<br>2026-09-19 글자수 세기 + Before/After 비교 요약 추가(코드 작성만, 미배포): 요구사항 1(실시간 글자수 카운터)은 기존 통계 카드(글자수 공백포함/공백제외)가 이미 `text` 상태 기반으로 실시간 반영하고 있어 별도 구현 불필요, 그대로 유지. `lib/textClean.ts`에 `cleanTextWithStats()` 신규 추가 — 줄바꿈 정리/띄어쓰기 교정을 같은 순서로 적용하되 각 단계 직후의 중간 결과를 서로 비교해 "그 단계가 실제로 몇 개를 정리했는지" 계산(정리 전/후 전체를 단순 비교하면, 줄바꿈 정리 과정에서 줄바꿈이 공백으로 치환되며 새로 생기는 공백까지 "정리된 공백"으로 잘못 잡히는 문제가 있어, 띄어쓰기 교정 단계는 "줄바꿈 정리 직후" 텍스트를 기준선으로 삼아 그 단계 자체의 순감소분만 셈). `page.tsx`에 `buildCleanSummary()` 추가해 "83자 → 72자 (11자 감소, 공백 2개·줄바꿈 0개 정리됨)" 형식의 요약 줄을 결과 화면(diff 하이라이트 박스) 바로 위에 표시, AI 프리미엄 교정 결과에는 이 요약이 뜨지 않도록 별도 분기 처리(정규식 기반 통계라 AI 교정에는 의미가 없음). 기존 노란 하이라이트 diff·체크박스 UI·검정/노랑 톤은 전혀 변경 없음. Node로 계산 로직 자체를 먼저 단위 검증(문단 병합 없는 케이스: 공백만 2개 감소 확인, 문단 병합 있는 케이스: 줄바꿈 5→2로 3개 감소, 글자수는 줄바꿈→공백 1:1 치환이라 변화 없음 확인 — 둘 다 수동 계산과 정확히 일치) 후 반영. 로컬 브라우저에서 실제 카톡 복사글 텍스트로 입력 시 실시간 카운터(83자)가 정확히 뜨는 것, 정리 버튼 클릭 후 "83자 → 72자 (11자 감소, 공백 2개·줄바꿈 0개 정리됨)" 요약이 Node 테스트와 동일하게 뜨는 것, 재편집 후 재입력 시 카운터가 다시 실시간으로 갱신되는 것(9자로 즉시 반영)까지 스크린샷·DOM 조회로 확인, 콘솔 에러 없음 |
| 02 | 마케팅 페이스메이커 | https://app-factory-marketing-pacemaker.vercel.app | 멀티채널 동시 진행 지원(카페/밴드/오픈채팅 복수 선택) → CampaignGroup(공통 닉네임/아이템/페르소나) + 채널별 Campaign(개별 링크/성격/진행일차). 대시보드: 1채널이면 기존 상세뷰+14일 타임라인, 2채널 이상이면 상단 요약 바("카페 D+7 · 밴드 D+3")+채널별 카드(미션가이드+복붙문구 AI생성+왕버튼 2개) 세로 나열, 채널별 독립 완료 체크. 14일 시나리오(가입인사→눈도장→화두던지기→일상공유→신뢰획득→미끼투척→D+15~회수클로징), Gemini API로 채널·페르소나별 맞춤 문구 실시간 생성 | TODO — 결제 게이트 미구현, 현재 무료 MVP 단계 (결제 시점은 추후 별도 지시서로 진행 예정) | - | 🟢 정상 — 완전히 새로운 GCP 프로젝트(gen-lang-client-0185524062, "앱 팩토리-02-프레시")에서 재발급한 키로 교체 후 AI 문구 생성 실제 복구 확인(2026-09-14). 온보딩/완료·취소/스트릭 등 나머지도 정상 | app-factory/02-marketing-pacemaker | 2026-08-31 최초 생성 시작<br>2026-08-31 MVP 구현 완료: 온보딩+대시보드+14일 시나리오+타임라인+Gemini 문구생성 API. 로컬에서 실제 API로 D1~D2 전 과정(문구 생성/복사+창열기/미션완료/D+15 클로징 전환) 실동작 검증 완료, 콘솔 에러 없음. 코드 작성 단계로 Vercel 배포는 진행 안 함<br>2026-09-02 온보딩 페르소나 선택형 전환: 자유서술형 페르소나/아이템소개 → AI 추천 페르소나 3~4개 탭 선택(수정 가능)+아이템 카테고리 선택형(8종+한줄보완)으로 교체, 5단계 위저드 UI로 재구성. `/api/generate-persona-options` 신규 라우트 추가(Gemini 호출, 실패시 폴백 3종 제공). Campaign에 persona_role/persona_tone 필드 확장(기존 필드 유지), 문구생성 프롬프트에도 페르소나 반영. 로컬에서 실제 API로 전 단계(채널→성격→페르소나 추천/선택→닉네임→카테고리→제출→대시보드 문구생성) 스텝별 라이브 검증 완료, 콘솔 에러 없음<br>2026-09-02 멀티채널 동시 진행 기능 추가: 데이터 모델에 CampaignGroup 신규(닉네임/아이템/페르소나 공통), 기존 Campaign은 group_id로 소속 + campaign_id 갖는 DailyMission으로 확장. 온보딩 채널 다중 선택(체크형)+채널별 링크/성격 순차 입력 루프 추가. 대시보드 ChannelCard 컴포넌트로 분리해 단일/멀티 뷰 모두 재사용. 레거시 단일 Campaign(구 pacemaker_campaign 키) 자동 마이그레이션 로직 구현. 로컬에서 (1) 레거시→그룹 마이그레이션 (2) 카페+밴드 2채널 동시 온보딩 전 과정 (3) 요약 바 실시간 갱신 (4) 채널별 독립 완료(카페만 D+2, 밴드는 D+1 유지) 전부 실동작 검증 완료, 콘솔 에러 없음<br>2026-09-02 스트릭 배지 추가 및 최초 배포: DailyMission에 completed_at(로컬 날짜) 추가, Campaign에 current_streak/longest_streak 추가(미션 완료 시점마다 자동 갱신). 연속 완료일수 계산 로직(하루 스킵 시 초기화)을 Node 단위테스트 7종(빈값/1일차/3일연속/중간공백/스킵후초기화/오늘미완료허용/동일날짜중복) 전부 통과로 검증. 배지는 2일째부터 옅은 회색 소배지, 3일 이상부터 "🔥 D+N일 연속 실행 중!" 강조 배지로 카드 헤더 우상단에 겹쳐 표시(왕버튼 레이아웃 변경 없음), 채널별 독립 계산. Vercel 최초 배포 진행<br>2026-09-03 Vercel 최초 배포 완료(CLI 직접 배포, `app-factory-marketing-pacemaker` 프로젝트, Git 연동 Root Directory 설정으로 저장소 루트에서 배포 필요). 배포 초기 GEMINI_API_KEY가 빈 값으로 등록되는 문제 발생(대화형 CLI 프롬프트에서 값이 누락되는 현상 반복) → 실시간 Vercel 로그로 "API key not valid" 직접 확인 후 원인 특정, 파이프 입력 방식으로 재등록 완료<br>2026-09-03 버그 수정: `/api/generate-content` 문구 생성 실패 시 무한 로딩/빈 화면 대신 안내형 폴백 문구 표시(온보딩 페르소나 추천과 동일한 graceful-degradation 패턴 적용), 서버 로그에 실패 원인 기록 추가. 완료된 날짜(D1~D14) 타임라인 클릭 시 확인창 후 완료 취소 가능하도록 UI 추가(취소 시 해당 일차 이후 미션 전부 미완료로 재조정, current_day 롤백, 스트릭 자동 재계산, longest_streak는 과거 최고 기록으로 유지). 로컬에서 실제 Gemini API로 온보딩 전 과정+문구 생성+재생성+완료 취소 전부 실동작 검증 완료(완료 취소는 브라우저 자동화 환경의 confirm() 억제 제약으로 window.confirm을 임시 패치해 우회 검증, 실제 사용자 환경에서는 정상 네이티브 확인창 노출), Vercel 재배포 후 실제 URL 실동작 검증 완료<br>2026-09-04 01호기 수준 경량화 리팩터링(코드 작성만, 미배포): 5단계 온보딩(채널선택→링크/성격→페르소나선택→닉네임→아이템카테고리)을 링크 입력창 1개+시작 버튼으로 압축한 `OnboardingLite` 신규 도입, `app/page.tsx`가 기본으로 이걸 사용하도록 전환. 채널유형은 URL 도메인 패턴 자동 판별(cafe.naver.com→카페, band.us→밴드, kakao.com→오픈채팅), 판별 실패 시에만 3버튼 폴백 선택 UI를 같은 화면에 조용히 노출(화면 전환 없음). 페르소나는 `/api/generate-persona-options` 응답의 첫 번째 추천을 자동 적용(선택 화면 제거), 닉네임은 기본값 랜덤 배정 — 둘 다 대시보드의 새 `CampaignSettings`(내 서비스·페르소나 설정, 접이식) 패널에서 언제든 수정 가능. 아이템 소개 카테고리 선택 단계 제거 → 같은 패널의 "내 서비스 소개(선택 입력)" 필드로 이동, 비워도 진행 가능(`/api/generate-content`의 필수 필드 체크에서도 제외, 프롬프트에서 비어있으면 아이템 언급 라인 자체를 생략). 기존 멀티채널 온보딩(`Onboarding.tsx`)은 삭제 대신 `MULTI_CHANNEL_ENABLED` 플래그로 비활성화만 하여 향후 프리미엄 기능으로 즉시 재활성화 가능. `Campaign`/`CampaignGroup`의 item_description·nickname·persona_role·persona_tone 필드는 삭제 없이 전부 옵셔널로 전환(레거시 데이터 손실 없음, 기존 값 그대로 로드). 스트릭 배지·완료 취소 기능 변경 없음. 로컬에서 (1) 카페 URL 자동판별→원클릭 대시보드 도달 (2) 판별 불가 텍스트 입력 시 폴백 선택 UI→선택 후 대시보드 도달 (3) 설정 패널 저장 후 localStorage 반영 전부 실동작 검증 완료, 콘솔 에러 없음. 배포는 진행하지 않음(현재 배포본은 이전 5단계 온보딩 버전 그대로)<br>2026-09-04 경량화 리팩터링 배포 및 검증: 로컬에서 `/api/generate-content`(실제 AI 응답 정상), "문구 다시 생성하기", 완료 취소(D+1 완료→취소→current_day/스트릭 정상 롤백) 전부 실동작 재확인 후 Vercel CLI로 프로덕션 재배포. 배포본에서도 링크 1개 온보딩→대시보드 원클릭 도달, 완료 취소(current_day 롤백 확인) 실동작 검증 완료, 콘솔 에러 없음. 단, Production GEMINI_API_KEY가 여전히 미해결 상태(`API key not valid`)라 배포본의 문구 생성은 안내형 폴백 문구로 동작 — 앱 자체는 정상, AI 실생성만 대기 중<br>2026-09-04 GEMINI_API_KEY 문제 최종 해결: Vercel 대시보드에서 기존 항목을 직접 편집하는 방식으로 재저장(Production+Preview 적용) 후 `vercel env pull`로 로컬 키와 길이·앞뒤 글자까지 정확히 일치함을 확인, 재배포 진행. 실시간 Vercel 로그와 배포본 실제 호출 결과 모두 진짜 Gemini 응답(폴백 아님) 확인 — `/api/generate-content`, `/api/generate-persona-options` 둘 다 자연스러운 실제 AI 문구 반환. 배포본 브라우저 실동작으로 온보딩→대시보드 원클릭 도달, 실제 AI 문구 생성, 완료→완료 취소(current_day 롤백)까지 전부 재검증, 콘솔 에러 없음. 앱 완전 정상화<br>2026-09-04 앱 아이콘 적용(코드 작성만, 미배포): 새 아이콘 세트(검정 배경+노란 심전도/펄스 라인, "페이스메이커" 컨셉)를 favicon.ico, icon-192.png, icon-512.png, icon-maskable-512.png, icon-180.png로 `public/`에 배치. `public/manifest.json` 신규 작성(192/512 purpose:any + 512 purpose:maskable). `app/layout.tsx` 메타데이터에 manifest·icons(icon 192/512, apple 180, shortcut favicon.ico) 명시 연결, 기존 파일 컨벤션 아이콘(`app/icon.svg`, 구디자인)은 새 세트와 충돌 방지를 위해 제거. 로컬 실행 후 `<link>` 태그 5종(manifest/shortcut icon/icon 192/icon 512/apple-touch-icon) 전부 정상 생성 및 각 파일 200 응답·manifest.json 파싱 정상까지 확인. 배포는 진행하지 않음(다음 배포 지시서에 함께 반영 예정)<br>2026-09-04 배포 전 10항목 통합 테스트 및 배포: 경량화 리팩터링+아이콘 적용 누적분을 로컬에서 처음부터 끝까지 이어서 검증 — (1)링크 자동판별→대시보드 원클릭 도달 (2)판별 실패→폴백 선택 UI (3)`/api/generate-content` 실제 AI 응답 (4)문구 재생성 (5)미션 완료 시 스트릭 배지(🔥 D+3일 연속) 반영 (6)완료된 날짜 클릭→취소→current_day·스트릭 정확히 롤백(longest_streak 보존) (7)설정 패널(서비스소개/닉네임/페르소나) 저장 후 새로고침에도 유지 (8)아이콘 5종 link 태그+manifest.json 정상 (9)콘솔 에러 없음(HMR 잡음 제외) (10)모바일 뷰포트(375x812)로 전체 플로우 재검증 — 10개 항목 전부 통과, 별도 수정 불필요. 통과 확인 후 Vercel CLI로 프로덕션 재배포, 배포본에서도 링크 입력→AI 문구 생성→완료→완료 취소 핵심 플로우 재확인 완료, 콘솔 에러 없음<br>2026-09-05 프리미엄 UI 표준화(코드 작성만, 미배포): 01호기와 동일한 `components/PremiumCapsule.tsx` 컴포넌트 이식(재사용 표준 컴포넌트, 저장소 루트 `PREMIUM_UI_STANDARD.md` 참고). 비활성화 상태인 멀티채널 온보딩(`Onboarding.tsx`, `MULTI_CHANNEL_ENABLED=false`라 현재 미사용)의 채널 선택 화면에 `onClick` 없는 안내용 캡슐("🔒 프리미엄 확장팩 (채널 3개 동시 운영) — 3,900원")을 상시 노출로 배치, `toggleChannel`을 수정해 채널 선택을 1개로 제한(2번째 선택 시도는 무시). "여러 개 선택 가능" 안내 문구도 "지금은 채널 1개로 시작할 수 있어요"로 교체. 로컬에서 플래그를 임시로 true로 켜서 (1)캡슐 상시 노출 (2)카페 선택 후 밴드 선택 시도 시 차단되고 카페만 유지 (3)선택 해제는 정상 동작 확인 후 플래그 다시 false로 복구, 콘솔 에러 없음. 기본 온보딩(`OnboardingLite`, 단일 링크 입력)은 변경 없음<br>2026-09-05 프리미엄 UI 표준화 배포 및 검증: 로컬에서 (1)`OnboardingLite` 링크 입력→대시보드 원클릭 도달 (2)문구 생성·완료·완료 취소·스트릭 배지(🔥 D+3일) 전부 회귀 없이 정상 (3)아이콘 5종 link 태그+manifest.json 정상 (4)콘솔 에러 없음 — 모바일 뷰포트(375x812)로도 동일 확인. Vercel CLI로 프로덕션 재배포 후 배포본에서도 온보딩→AI 문구 생성→완료→완료 취소, 아이콘 link 태그 전부 재확인 완료, 콘솔 에러 없음<br>2026-09-05 Vercel Web Analytics 연동(01호기와 동일 방식): `@vercel/analytics` 패키지 설치, `app/layout.tsx`에 `<Analytics />`(from `@vercel/analytics/next`) 추가. 로컬 빌드 정상, 기존 UI·콘솔 에러 없음 확인 후 Vercel CLI로 프로덕션 배포 완료<br>2026-09-14 AI 생성 실패 시 내부 에러 노출 버그 긴급 수정: 실시간 Vercel 로그로 진단한 결과 `/api/generate-content`가 `401 UNAUTHENTICATED / ACCOUNT_STATE_INVALID`(서비스 계정 비활성화, 01호기와는 별개의 GEMINI_API_KEY)로 실패 중 — Google Cloud 계정 문제라 코드로 조치 불가, 대장님 별도 확인 필요. 핵심 버그는 이 실패 시 서버가 `[자동 생성 실패 - 기본 문구입니다]`라는 내부 마커 텍스트를 정상 200 응답의 `content` 필드에 그대로 담아 보내던 것 — 사용자가 이를 실제 게시문구로 착각해 커뮤니티에 복붙할 위험이 있었음(D+9에서 실제 발생 확인). `/api/generate-content`의 모든 실패 분기를 `{content: 폴백텍스트}` 200 응답 대신 `{error: "AI 생성에 실패했습니다."}` 502/500 에러 응답으로 변경(`buildFallbackContent` 함수 자체 제거). `ChannelCard.tsx`에 `generationFailed` 상태 추가 — 실패 시 복붙 박스는 완전히 비우고(단, 기존에 이미 채워져 있던 유효한 문구가 있으면 지우지 않고 그대로 보존), 박스 바깥에 "⚠️ AI 생성에 실패했어요. 직접 작성하거나 다시 시도해주세요." 안내를 골드 톤으로 표시, "문구 복사하고 커뮤니티로 이동" 버튼은 내용이 비어있으면 자동으로 비활성화(disabled) 처리. 왕버튼 2개 구조·기존 UI는 변경 없음. 로컬에서 실제로 살아있는 502 상황(Google 계정 문제)으로 재현해서 (1) 복붙 박스에 내부 에러 문구 절대 노출 안 됨, 완전히 빈 상태 (2) 경고 문구 정상 노출 (3) 복사 버튼 비활성화 (4) fetch 모킹으로 성공 후 재시도 실패 시나리오에서 기존 유효 문구는 보존되고 버튼도 다시 활성화됨 — 전부 확인, 데스크톱+모바일(375x812) 둘 다 확인. 완료/취소 등 기존 기능 회귀 없음, 새로운 클라이언트 콘솔 에러 없음(502 네트워크 로그는 Google 계정 문제로 인한 기존 현상). Vercel CLI로 프로덕션 배포 완료<br>2026-09-14 GEMINI_API_KEY 재발급 반영 검증 — 여전히 막힘, 근본 원인 특정: `vercel env pull`로 Production 키가 로컬과 정확히 일치함을 확인(길이/앞뒤 5자 동일) 후 확실한 반영을 위해 재배포. 실시간 Vercel 로그로 재현한 결과 이번엔 `403 PERMISSION_DENIED / CONSUMER_SUSPENDED`("Consumer 'api_key:AQ.Ab8RN6Kk7q...'has been suspended", consumer projects/992228513660) — **01호기가 겪었던 것과 정확히 같은 Google Cloud 프로젝트 번호**. 즉 새로 발급한 키 자체는 반영이 잘 됐지만, 그 키가 속한 GCP 프로젝트가 여전히 정지 상태라 소용없음이 확인됨. 같은 정지된 프로젝트 안에서는 어떤 키를 새로 발급해도 계속 막힐 것으로 예상 — 완전히 다른, 정상 상태인 GCP 프로젝트에서 키를 발급받아야 근본적으로 해결됨. 지난번 수정한 폴백 UI(실패 시 박스 비움+경고+복사버튼 비활성화)는 이 배포본에서도 정상 작동 재확인, fetch 모킹으로 정상 성공 케이스도 회귀 없이 실제 문구를 잘 채우는 것까지 확인, 새 콘솔 에러 없음. **작업 중 실수 발견**: 검증 재배포 시 실수로 저장소 루트에서 `--project` 플래그 없이 `vercel --prod`를 실행해 의도치 않게 `app-factory`라는 별개의 신규 Vercel 프로젝트가 생성/배포됨(`app-factory-chi.vercel.app`) — 즉시 발견하여 올바른 `app-factory-marketing-pacemaker` 프로젝트로 재배포 완료했으나, 잘못 생성된 `app-factory` 프로젝트는 삭제 승인을 받아야 해서 그대로 남아있음(대장님 확인 필요)<br>2026-09-14 GEMINI_API_KEY 완전히 새로운 GCP 프로젝트로 재발급 반영 검증 — 근본 해결 확인: 대장님이 Google AI Studio에서 기존 정지된 프로젝트(projects/992228513660)와는 완전히 다른 신규 GCP 프로젝트(gen-lang-client-0185524062, "앱 팩토리-02-프레시")에서 새 키를 발급받아 Vercel Production에 반영. `vercel env pull`로 반영 확인 후 저장소 루트에서 `vercel link --project app-factory-marketing-pacemaker`로 명시적 재확인 후 배포(지난 실수 재발 방지 — 이번엔 `--project` 플래그와 링크 결과를 매번 검증). 재배포 후 실시간 Vercel 로그로 확인한 결과 401/403은 사라졌으나 새로운 오류 발견 — `models/gemini-2.5-flash is no longer available to new users` (404, 신규 GCP 프로젝트라 구모델 접근 불가). `/api/generate-content`, `/api/generate-persona-options` 두 라우트의 모델명을 `gemini-3.6-flash`로 교체(Gemini API 자체 에러 메시지가 안내한 대체 모델). 로컬에서 curl로 신모델 직접 호출 성공(200) 확인 후 배포, 배포본에 실제 POST 요청 재현 결과 진짜 AI 생성 문구 정상 반환(HTTP 200, Vercel 로그에도 에러 없음). 브라우저로 "문구 다시 생성하기" 클릭 재검증 — 매번 새로운 실제 문구 생성, 실패 경고 없음, 복사 버튼 정상 활성화. 폴백 UI(실패 시 박스 비움+경고+버튼 비활성화) 로직은 이번엔 트리거되지 않음(실제로 성공했으므로) — 이전 검증들에서 이미 실패 경로도 별도 확인됨. 상태 🟢로 정상 갱신. 잘못 생성됐던 `app-factory` 프로젝트는 대장님 승인 받아 삭제 완료(2026-09-14, `vercel project rm app-factory`로 제거, `app-factory-marketing-pacemaker` 등 나머지 프로젝트 목록에서 정상 확인) |
| 03 | 숏폼 대본 공장 (패스트레인 클론) | (코드 작성만 — 미배포) | 패스트레인 핵심 3단계 복제. (1) URL 입력 1개 → `/api/analyze-content`가 페이지 크롤링(정규식 기반 텍스트 추출, private/localhost 호스트 차단) 후 Gemini로 타깃 고객·핵심 고통(Pain Point) 3~5개·핵심 제공가치 추출. (2) 틴더식 스와이프 — 검증된 바이럴 훅 구조 10종(충격 고백형/극단적 대조형/경고형/질문 도발형/반전 서사형/통계 충격형/타겟 저격형/비밀 공개형/실패담 공감형/초스피드 해결형)을 `/api/generate-scripts`가 Gemini 구조화 출력(responseSchema)으로 한 번에 완성형 대본 10편 생성(훅→고통자극→시연가이드→CTA), ❌거절/⭕선택(왕버튼) 카드 UI로 1편씩 스와이프. (3) 선택한 대본마다 0초 썸네일 볼드 문구 2줄·35초 영상 대본·유튜브쇼츠/릴스/틱톡 공용 캡션+해시태그를 항목별 📋복사 버튼과 함께 패키징 화면에 표시. 우측 하단 고정 플로팅 버튼 "📞 김반장과 10분 통화하고 무료 이용권 받기"(카카오 오픈채팅 연결, `NEXT_PUBLIC_KAKAO_OPENCHAT_URL` 미설정 시 안내만 표시 — 실제 링크는 대장님이 채워야 함). 검정/노랑 고대비, 큰 버튼 위주 미니멀 UI(50대 이상 기준) | 결제 로직 미연동 — `PremiumCapsule` 컴포넌트를 01/02호기와 동일 코드로 이식해 패키징 화면에 "프리미엄 확장팩 (대본 무제한 생성 + 채널별 캡션 분리) — 3,900원" 안내용(onClick 없음)으로 상시 노출만 함 | - | 🟢 정상 — 로컬(포트 3003)에서 실제 위키백과 URL로 분석→대본 10편 생성→스와이프 5편 선택→패키징 화면(썸네일/대본/캡션+해시태그 복사)까지 전체 플로우 실동작 검증 완료 | app-factory/03-fastlane-clone | 2026-09-15 최초 생성: Next.js 16+React 19+Tailwind v4로 01/02호기와 동일 스택 구성, `GEMINI_API_KEY`는 기존 앱들과 같은 키를 `.env.local`에 복사(로컬 개발용, git 미추적). 3단계 전체 구현 후 로컬 실동작 검증 중 스와이프 카드의 나가는 애니메이션이 `setTimeout` 220ms 지연 후 `onDecision` 콜백을 호출하는 구조였는데, 이 타이머가 (코드 편집으로 인한 Fast Refresh 등으로) 유실되면 카드가 반투명·화면 밖 상태로 멈춘 채 버튼이 계속 비활성 상태(`disabled`)로 남아 영원히 진행 불가능해지는 결함을 실제로 재현함. `SwipeStep.tsx`를 타이머에 의존하지 않는 구조로 재작성 — 클릭 시 `onDecision`을 즉시 동기 호출(중복 클릭은 `useRef` 플래그로 방지)하고, 카드 등장 애니메이션은 결정 로직과 완전히 분리된 마운트 시점 트랜지션으로 변경해 타이머 유실로 인한 멈춤 가능성 자체를 제거. 또한 우측 하단 고정 통화 버튼이 스와이프 화면의 ❌거절/⭕선택 버튼과 짧은 화면에서 겹쳐 클릭이 잘못 인식되는 문제를 발견해 각 화면 컨테이너에 하단 여백(`pb-28`)을 추가해 해결. 수정 후 로컬에서 URL 입력→분석 로딩→대본 10편 생성 로딩→10편 전체 스와이프(선택 5/거절 5)→패키징 화면 진입→복사 버튼(클립보드 실제 기록 확인)→플로팅 통화 버튼(링크 미설정 안내 확인)→처음부터 다시하기(초기화 확인)까지 전 구간 재검증 완료, 콘솔 에러 없음. Gemini 일시 과부하(503) 상황도 실제로 재현되어 에러 배너가 정상 노출됨을 확인. `.claude/launch.json`(저장소: ma_unse)에 `03-fastlane-clone`(포트 3003) 프리뷰 설정 추가. 배포는 진행하지 않음(지시서상 "코드 작성만")<br>※ 이 칸은 이후 여러 지시서(과금 모델 전환/SNS 딥링크/Vrew 4클립/3채널 SEO 카드/Footer·약관 페이지/토스페이먼츠 테스트 결제창 등)에서 갱신이 누락되어 있었음 — git log가 최신 상태의 정확한 출처<br>2026-09-20 "1회 맛보기 + 결제 락 + 7일 매직링크" 이용 정책 통합 구현 및 배포: `lib/trial.ts` 신규(localStorage `fastlane_trial_used`로 1회 무료 체험 추적, `free_trial_until` 타임스탬프로 7일 매직 패스 관리, 만료 시 저장값 자동 정리). `?pass=free7day` 접속 시 `activateMagicPass()`로 7일 타임스탬프 저장, 상단 `TrialStatusBanner`(전 화면 공통, page.tsx 최상단)에 "🎉 김반장 7일 무료 이용권 적용 중 (D-N일 남음)" 노출. 일반 접속자는 `handleAnalyze` 진입 시 `canGenerate()` 체크 — 1회 체험 완료 후 재시도하면 API 호출 자체를 하지 않고(비용 절감) `PaywallModal` 신규 컴포넌트로 즉시 차단(기존 `ProPassPaymentButton` 재사용 + "김반장과 10분 상담" 카카오 오픈채팅 버튼). 대본 생성 성공 시 매직 패스 보유자가 아닌 경우에만 `markFreeTrialUsed()` 호출. 기존 "오픈 기념 100% 무료" 배지는 새 정책과 모순되어 `PackageStep.tsx`에서 완전 제거. `CallFloatingButton`의 카카오 오픈채팅 오픈 로직을 `lib/kakao.ts`로 추출해 `PaywallModal`과 공유(동작 변경 없음). 로컬(3003)에서 (1) 첫 방문 맛보기 클릭 → 정상 생성 + `fastlane_trial_used=1` 기록 (2) 새로고침 후 재시도 → API 호출 없이 즉시 결제 모달 노출(서버 로그로 `/api/analyze-content` 미호출 확인) (3) `?pass=free7day` 접속 → D-7일 배너 노출 + 이미 소진된 1회 제한 무시하고 정상 생성, 패키징 화면까지 배너 유지·기존 Vrew/SEO 카드 기능 회귀 없음 확인 (4) `localStorage.free_trial_until`을 과거 시각으로 강제 조작(만료 시뮬레이션) 후 새로고침 → 배너 자동 소멸 + 저장값 자동 정리(`null`) + 재생성 시도 시 결제 모달로 정확히 복귀 — 4개 시나리오 전부 실제 클릭·DOM 조회로 확인, 콘솔 에러 없음. `npm run build` 통과 후 git push + `vercel --prod` 배포 완료, 프로덕션(`https://03-fastlane-clone.vercel.app`)에서도 동일 4개 시나리오(1회 체험→결제 모달, 매직 링크 D-7일 배너, 만료 시뮬레이션→자동 일반 모드 복귀) 전부 재검증 완료, 콘솔 에러 없음<br>2026-09-22 Vercel Web Analytics 연동 지시서 확인 — 이미 완료 상태였음: "코드에 Analytics 컴포넌트가 없다"는 지시서 전제를 코드로 직접 검증한 결과 사실과 다름을 확인 — `app/layout.tsx`의 `<Analytics />`(from `@vercel/analytics/next`)와 `package.json`의 `@vercel/analytics@^2.0.1`이 **03호기 최초 생성 커밋(`c3e9536`, 2026-09-15)부터** 이미 포함되어 있었음(01/02호기 레이아웃 패턴을 그대로 복제하는 과정에서 함께 들어간 것으로 추정, npm 최신 버전도 2.0.1로 동일해 업그레이드 대상도 없음). 실제 프로덕션에 접속해 네트워크 탭으로 직접 확인한 결과 Analytics 비콘 스크립트와 조회 이벤트(`POST .../view`)가 이미 200 OK로 정상 전송되고 있음을 확인 — 코드는 처음부터 정상 작동 중이었고, 유일하게 비어있던 것은 Vercel 대시보드의 프로젝트별 Analytics `Enable` 토글(코드로 조작 불가한 대시보드 설정)뿐이었음. 대장님이 대시보드에서 직접 Enable을 누르신 뒤 이 사실을 확인했으므로, 코드 변경·커밋·재배포는 진행하지 않음(변경사항이 없는데 형식상 빈 커밋을 만드는 것은 이력만 지저분하게 만든다고 판단) |
| 04 | SNS 바이럴 피더 (스레드 & 스토리 전용) | https://04-sns-viral-feeder.vercel.app | 03호기와 완전히 독립된 신규 앱. 릴스 주제/키워드 1개 입력 → 큰 노란 버튼 한 번으로 `/api/generate-feed`가 Gemini(`gemini-3.5-flash-lite`, responseSchema 구조화 출력)를 호출해 인스타·스레드 알고리즘 노출 공식에 맞춘 카드 6개를 한 번에 생성: (1) 스레드 본문 원고 — 현장 근무자 특유의 구수하고 신뢰감 있는 구어체(연차를 구체적 숫자로 언급하지 않음), 첫 줄 단정/도발형 후킹(질문형 금지), 3~4문장+줄바꿈 넉넉, 해시태그 도배 대신 본문에 키워드 자연 녹임, 본문 내 링크 금지, 마지막 문장은 댓글 유도 질문 (2) 스레드 첫 댓글 링크 — AI가 만든 리드 문장 뒤에 03호기 매직링크 URL(`https://03-fastlane-clone.vercel.app/?pass=free7day`)을 서버에서 정확히 고정 삽입 (3) 인스타 스토리 세팅 가이드 — 텍스트 스티커 문구(15자 내외) + 매직링크 URL + 링크 라벨 "👉 7일 무료 이용권" (4) 릴스 고정 댓글 — 프로필 링크 안내 2줄 (5) 릴스 캡션 — 인스타 SEO 최적화(첫 125자 안에 핵심 검색 키워드 집중 배치 + "저장해두고 필요할 때 꺼내 쓰세요" 저장 유도 문구 포함) (6) 댓글/DM 유도 트리거 — 주제에 맞는 키워드로 댓글 유도. 모든 링크 카드의 목적지 URL은 서버에서 상수로 고정해 AI가 URL 자체를 생성하지 않도록 원천 차단. 마크다운 기호는 프롬프트로 금지하고 서버에서 한 번 더 방어적으로 제거(`stripMarkdown`)해 순수 텍스트로만 출력. 복사 버튼 클릭 시 하단 고정 토스트 "복사 완료!" 즉시 노출. 검정/노랑 고대비, 왕버튼, 모바일(375px) 잘림 없는 스크롤 레이아웃 유지(카드 제목 배지에 `whitespace-nowrap` 적용해 375px에서 배지 내부 줄바꿈 없이 복사 버튼만 다음 줄로 자연스럽게 내려가도록 개선) | 결제 기능 없음 — 03호기 유료 전환을 유도하는 무료 확산 도구 (별도 과금 없음) | - | 🟢 정상 — 로컬(포트 3004)과 프로덕션(`https://04-sns-viral-feeder.vercel.app`) 양쪽에서 실제 키워드로 Gemini 실호출→6카드 전부 정상 생성→매직링크 URL 두 카드(첫 댓글 링크/스토리 가이드) 모두 정확히 고정→원클릭 복사(클립보드 성공 시나리오 포함 실검증)→토스트 노출까지 확인, 모바일 뷰포트(375x812)에서 로컬·프로덕션 둘 다 카드 6개 전부 잘림 없이 표시 확인, 콘솔 에러는 이 브라우저 자동화 환경 특유의 클립보드 권한 제약(`NotAllowedError`, 기존 앱들과 동일한 환경 한계)만 로컬에서 존재하며 프로덕션에서는 그마저도 관찰되지 않음 | app-factory/04-sns-viral-feeder | 2026-09-21 최초 생성(코드 작성만, 미배포): Next.js 16+React 19+Tailwind v4로 01~03호기와 동일 스택 신규 프로젝트 구성(`GEMINI_API_KEY`는 기존 앱들과 같은 키를 `.env.local`에 복사, git 미추적). `lib/gemini.ts`(01~03호기와 동일 패턴), `lib/types.ts`(ViralFeed 타입 + 03호기 매직링크 URL/라벨 상수), `app/api/generate-feed/route.ts`(스레드 SEO 규칙·김반장 페르소나·마크다운 금지 프롬프트 + responseSchema + 서버측 마크다운 방어적 제거), `components/CopyButton.tsx`(01~03호기 패턴 이식 + onCopied 콜백 추가), `components/Toast.tsx`(신규, 하단 고정 토스트), `components/ThreadCard.tsx`/`StoryGuideCard.tsx`(신규), `app/page.tsx`(입력창+생성 버튼+결과 카드 2개+토스트 조립). `npm run build` 통과. `.claude/launch.json`(저장소: ma_unse)에 `04-sns-viral-feeder`(포트 3004) 프리뷰 설정 추가. 로컬에서 (1) 키워드 입력→생성 버튼→스레드 원고(단정형 후킹·줄바꿈 3~4문장·해시태그 미도배·본문 링크 없음·프로필 링크 유도 마무리 문장 전부 규칙 충족)+스토리 문구(8자, 15자 이내) 정상 생성 (2) 원클릭 복사 클릭 시 클립보드 성공 경로에서 하단 토스트 "복사 완료!" 즉시 노출(이 자동화 환경은 클립보드 쓰기 권한이 기본 차단되어 있어 `navigator.clipboard.writeText`를 임시로 패치해 성공 경로까지 확인, 실제 사용자 브라우저에서는 권한 문제 없이 정상 작동 예상 — 01~03호기와 동일하게 문서화된 환경 한계) (3) 모바일 뷰포트(375x812)에서 입력창·버튼이 최상단에 엄지 범위 내로 배치되고 결과 카드도 검정/노랑 고대비 유지한 채 잘림 없이 표시 — 전부 스크린샷·DOM 조회로 확인, 앱 자체 코드로 인한 콘솔 에러 없음. git 로컬 커밋만 진행(push/배포 없음, 지시서상 "코드 작성만")<br>2026-09-21 인스타·스레드 알고리즘 맞춤 6카드 고도화 및 배포: 대장님이 실제 경력(1년차)과 다른 "30년 현장 김반장" 표현을 문서에서 발견해 지시서에서 정정 — 페르소나 톤("구수하고 신뢰감 있는 현장 목소리")은 유지하되 "30년" 등 구체적 연차 숫자는 절대 언급하지 않도록 프롬프트에 명시적으로 반영(작업 전 기존 코드에 "30년"·"년차"·"경력" 문자열이 전무함을 grep으로 먼저 확인). 기존 2카드(스레드 원고/스토리 가이드) 구성을 6카드로 확장: `lib/types.ts`의 `ViralFeed`에 threadFirstComment/reelsPinnedComment/reelsCaption/commentDmTrigger 4개 필드 추가. `app/api/generate-feed/route.ts` — Gemini 스키마와 프롬프트를 6개 항목으로 확장하면서, 링크가 들어가는 항목(스레드 첫 댓글)은 AI에게 URL을 직접 쓰지 않고 화살표 이모지 "👉"로 문장을 끝내는 "리드 문장"만 생성하게 한 뒤 서버 코드가 `STORY_LINK_URL` 상수를 문자열로 이어붙이는 방식으로 설계 — AI가 URL을 잘못 재현하거나 다른 링크를 넣을 가능성 자체를 차단해 "매직 링크 URL 정확히 고정" 요구사항을 코드로 보장. 재사용 가능한 `components/TextCard.tsx` 신규 작성(아이콘/제목/본문/복사 라벨 props)으로 카드 1·2·4·5·6을 전부 커버, 기존 전용 `ThreadCard.tsx`는 삭제하고 대체. `app/page.tsx`에 6카드를 순서대로(본문→첫 댓글→스토리 가이드→고정 댓글→캡션→DM 트리거) 배치. 1차 로컬 검증 중 375px 모바일에서 "스레드 첫 댓글 링크"·"릴스 캡션 (SEO 125자 최적화)" 배지가 길어서 내부적으로 2줄로 줄바꿈되는 사소한 레이아웃 문제를 발견 — `TextCard`/`StoryGuideCard`의 헤더 행에 `flex-wrap`을, 배지 자체에 `whitespace-nowrap`을 추가해 배지는 한 줄 유지하고 복사 버튼만 필요 시 다음 줄로 내려가도록 수정, 재빌드 후 로컬 375px에서 모든 배지가 한 줄로 정상 표시됨을 재확인. `npm run build` 통과 후 실제 소스 파일만 이름으로 지정해 git 커밋(`8eabc47`) 후 `git push origin main` 완료. Vercel 프로젝트 최초 링크(`vercel link --project 04-sns-viral-feeder`, `sonesons-projects/04-sns-viral-feeder` 생성) 후 `GEMINI_API_KEY`를 Production 환경변수로 신규 등록(`vercel env ls production`으로 등록 확인), `vercel --prod` 배포 완료 후 `https://04-sns-viral-feeder.vercel.app`으로 별칭 연결. 로컬(3004)·프로덕션 양쪽에서 서로 다른 키워드("퇴근 후 부업 추천" 등)로 (1) 6카드 전부 정상 노출 (2) 스레드 첫 댓글 링크·스토리 가이드 두 카드 모두 매직링크 URL이 `https://03-fastlane-clone.vercel.app/?pass=free7day`로 정확히 일치 (3) 전체 텍스트에 마크다운 기호(`**`, `*`, `#` 등)나 "30년" 등 연차 표현이 전혀 없음 (4) 375px 모바일에서 카드 6개 전부 잘림 없이 스크롤로 확인 — 전부 스크린샷·DOM 조회(`get_page_text`)로 재검증 완료. 콘솔 에러는 로컬에서만 클립보드 권한 제약(`NotAllowedError`, 기존 앱들과 동일한 자동화 환경 한계) 존재, 프로덕션에서는 콘솔 로그 자체가 없음(`No console logs.`)<br>2026-09-24 PAS 공식 강제 + 진정성 팩트 프롬프트 고도화 및 배포: 기존 프롬프트는 "도발형 첫줄 후킹 + 구수한 톤"까지만 지시해 Problem-Agitation-Solution 구조가 명시적으로 강제되지 않고 구체적 팩트도 없어 생성 원고가 매끈한 광고 카피처럼 나오던 문제를 개선. `app/api/generate-feed/route.ts`의 threadPost·reelsCaption 두 프롬프트 항목에 [Problem]/[Agitation]/[Solution] 3단 구조를 명시적으로 라벨링해 강제(1문장 문제 제시→1~2문장 자극→해결 문장, 마지막은 Solution에서 자연스럽게 이어지는 대화 유도 질문). 시스템 지침에 "1년차 건설 현장 근무자가 퇴근 후 밤마다 AI 자동화를 독학하며 만든 도구"라는 배경을 추가하되, "6개 항목 중 가장 자연스러운 1~2곳에서만 스치듯 녹이고 나머지는 언급하지 않는다"는 빈도 제한을 명시해 매 항목 반복으로 인한 광고스러움을 방지. "30년" 등 사실과 다른 연차 숫자 금지 규칙은 그대로 유지. `npm run build` 통과 후 로컬에서 이전 버전과 동일 키워드("숏폼 대본 자동 생성")로 재생성 비교 — 이전엔 Solution 위주로 뭉뚱그려진 원고였던 반면, 신규 버전은 Problem("퇴근하고 집에 와서 숏폼 대본 하나 짜려다 멍하게 모니터만 바라보는 날이 하루 이틀이 아입니더")→Agitation(다음날 현장 피로 누적 묘사)→Solution(자동화 도구로 해결 + "밤마다 머리 싸맬 필요 없이... 맹글었더만"으로 팩트 자연스럽게 1회 언급)으로 구조가 뚜렷하게 구분됨을 확인. 이 1차 생성에서 threadPost 마지막 질문 문장에 `해결하십니icka?` 같은 Gemini 자체 샘플링 글리치(영문 음절 혼입)가 1회 관찰됐으나, 동일 세션에서 다른 키워드("퇴근 후 부업 추천")로 재생성 시 글리치 없이 정상 문장으로 재현되지 않음을 확인 — 프롬프트나 코드가 유발한 결함이 아니라 LLM의 드문 샘플링 이상으로 판단, 별도 코드 수정 없이 관찰 사실만 기록. git 커밋(`28ec2d0`) 후 `git push origin main` + `vercel --prod` 배포 완료, 프로덕션(`https://04-sns-viral-feeder.vercel.app`)에서도 새 키워드("1인 마케팅 자동화 꿀팁")로 PAS 구조 뚜렷함과 팩트 1~2회 자연스러운 반영("퇴근하고 밤마다 머리 싸매며 직접 만든...") 재확인, 매직링크 URL 두 카드 모두 정확히 고정, 마크다운 잔재 없음, 콘솔 에러 없음(`No console logs.`)<br>2026-09-24 음성 입력 + 블로그 카드 확장(3분할 OSMU 공정) 및 배포: 기존 텍스트 키워드 입력 6카드 구조에 (1) 브라우저 MediaRecorder 기반 음성 녹음 입력, (2) 네이버 블로그(850자)·워드프레스(1500자) 카드 2종을 추가해 "음성 하나 → 쇼츠 대본·스레드·블로그" 8개 콘텐츠 동시 생성 공정으로 확장. `components/VoiceRecorder.tsx` 신규 — 녹음 시작/중지 버튼(빨간 펄스 애니메이션), 정지 시 오디오 Blob을 base64로 변환해 신규 `app/api/transcribe/route.ts`에 전달. transcribe 라우트는 Gemini `generateContent`에 `inlineData`(mimeType+base64)로 오디오를 직접 전달해 전사만 반환(설명 문구 없이 전사 결과만 출력하도록 프롬프트 고정), 전사 성공 시 `page.tsx`가 결과를 기존 `topic` state에 그대로 채우고 텍스트 탭으로 자동 전환해 사용자가 검토·수정 후 그대로 생성 버튼을 누를 수 있도록 연결(음성/텍스트 두 입력 경로가 동일한 생성 파이프라인을 공유). 입력 상단에 "⌨️ 텍스트 / 🎙️ 음성" 토글 추가, 생성 버튼 문구를 "1초 만에 쇼츠·블로그·스레드 3갈래 뽑기"로 변경. `app/api/generate-feed/route.ts`에 naverBlogBody(7번)·wordpressBody(8번) 프롬프트 항목 추가 — 네이버는 첫 줄에 `[네이버 블로그 제목: ○○○]` SEO 제목 필수, 구수한 사투리 일기체, 본문은 750자 안쪽으로 여유를 두도록 지시(전체 850자 이내). 워드프레스는 전문 용어 톤에 마크다운 대신 `<h2>/<h3>/<p>` HTML 태그만 사용하도록 지시, 본문은 1300자 안쪽 여유(전체 1500자 이내). 두 카드 모두 AI가 마무리 CTA·링크를 직접 쓰지 않게 금지하고, 매직링크 URL은 기존 카드들과 동일하게 서버 코드가 고정 문자열(`NAVER_CLOSING`/`WORDPRESS_CLOSING` 상수)로 항상 이어붙이는 구조를 유지해 URL 오재현 가능성을 원천 차단. `lib/types.ts`의 `ViralFeed`에 naverBlogPost/wordpressPost 필드 추가. 최초 구현 중 프롬프트 문자열 안에 리터럴 백틱(`` ` ``)을 그대로 써서 템플릿 리터럴이 조기 종료되는 빌드 에러가 발생 — 백틱을 작은따옴표/평문으로 교체해 해결. `npm run build` 통과 후 로컬에서 실제 TTS로 만든 짧은 음성 파일(WAV)을 `/api/transcribe`에 직접 호출해 전사 파이프라인이 실제로 Gemini와 연동되어 정상 작동함을 확인(마이크 자체는 이 Claude Browser 자동화 환경에서 `getUserMedia`가 차단되어 있어 — 클립보드와 동일한 샌드박스 제약 — 브라우저 클릭을 통한 실제 녹음 UI 끝까지는 밟지 못했으나, 권한 거부 시 에러 메시지가 뜨고 idle 상태로 정상 복귀하는 것과 엔드포인트 자체의 정상 동작을 각각 확인해 전체 파이프라인이 코드 레벨에서 작동함을 검증). 텍스트 입력 경로로 로컬에서 8카드 전부 생성 확인(네이버 484자, 워드프레스 889자, 둘 다 제한 이내). **1차 검증 중 실제 결함 발견**: 스레드 본문 마지막 질문 문장에 일본어 "仕組み(시스템)"가 한국어 문장에 섞여 나오는 것을 프로덕션 배포 후 재검증 과정에서 발견 — 시스템 프롬프트에 "모든 출력은 순수 한국어로만 작성, 일본어·중국어·영어 혼용 금지" 지시를 명시적으로 추가해 재배포, 이후 로컬 2회·프로덕션 1회 총 3회 재생성을 정규식(`/[぀-ヿ一-鿿]/`)으로 자동 검사해 전부 외국어 혼입 없음을 확인. git 커밋 3건(`ff8759b` 기능 추가, `4adfc5e` 외국어 방지 수정, 이후 app_inventory 갱신 커밋) 후 각각 `git push origin main` + `vercel --prod` 배포 완료. 프로덕션(`https://04-sns-viral-feeder.vercel.app`)에서 서로 다른 키워드 3종으로 8카드 전부 정상 생성, 네이버/워드프레스 글자수 제한 준수(490자/761자 등), 매직링크 URL 두 블로그 카드 모두 정확히 고정, 콘솔 에러 없음(`No console logs.`) 재확인 완료<br>2026-09-24 블로그 카드 제거(6카드 단순화) + 채널별 UTM 추적 적용 및 배포: 블로그 기능은 이미지·SEO까지 갖춘 05호기(별도 신규 앱)로 제대로 만들 예정이라 04호기의 네이버(850자)·워드프레스(1500자) 카드 2종을 완전히 제거하고 원래의 6카드(스레드 본문/첫댓글/스토리가이드/릴스고정댓글/SEO캡션/DM트리거) 구조로 복귀. `lib/types.ts`의 `ViralFeed`에서 naverBlogPost/wordpressPost 필드 삭제, `app/api/generate-feed/route.ts`의 FEED_SCHEMA·RawFeed·프롬프트 7·8번 항목·`NAVER_CLOSING`/`WORDPRESS_CLOSING` 상수 전부 삭제, `app/page.tsx`에서 두 블로그 카드 JSX 및 생성 버튼 문구("1초 만에 쇼츠·블로그·스레드 3갈래 뽑기"→"1초 만에 스레드·스토리 원고 뽑기")·헤더 부제("쇼츠·블로그·스레드 3갈래 동시 생성"→"스레드 & 스토리 전용")를 원상 복구. 지시서가 언급한 "상대 글 URL 입력칸(네이버-워드프레스 상호링크용)"은 코드에 애초부터 존재한 적이 없었음을 확인 — 애초 설계 시 매직링크를 사용자 입력이 아닌 서버 코드가 고정 상수로 항상 이어붙이는 방식으로 만들었기 때문에 그런 입력칸 자체가 필요 없었던 것으로, 지시서 전제와 실제 코드가 달라 해당 항목은 별도 삭제 없이 넘어감. 음성 녹음 입력(`VoiceRecorder.tsx`, `/api/transcribe`)은 이번 작업에서 전혀 건드리지 않고 그대로 유지. 채널별 UTM 추적: `lib/types.ts`에 `BASE_URL`+공통 `UTM_COMMON`(`utm_medium=organic&utm_campaign=04ho_content_factory`)을 두고 `THREADS_LINK_URL`(`utm_source=threads`)·`STORY_LINK_URL`(`utm_source=story`)·`REELS_LINK_URL`(`utm_source=reels`) 3개 상수로 분리. 스레드 그룹의 링크는 기존처럼 "스레드 첫 댓글" 카드에, 스토리 그룹은 기존 `StoryGuideCard`에 그대로 나타나던 자리에 자동으로 utm 태그가 반영되도록 구성. 릴스 그룹(고정댓글/캡션/DM트리거 3장) 중에는 실제 URL 문자열이 필요한 카드가 "릴스 고정 댓글" 하나뿐이라(캡션·DM트리거는 원래부터 URL을 담지 않는 순수 안내문 구조) 서버 코드가 `reelsPinnedComment` 끝에 `REELS_LINK_URL`을 새 줄로 붙이는 방식으로 구현 — 채널 그룹당 대표 카드 1곳에만 실제 링크가 노출되는 기존 패턴(스레드=첫댓글, 스토리=가이드카드)과 동일한 논리를 릴스 그룹에도 일관되게 적용한 것. `npm run build` 통과 후 로컬·프로덕션 각각에서 서로 다른 키워드로 6카드만 노출되는지, 3개 링크의 utm_source가 threads/story/reels로 정확히 구분되고 공통 파라미터(`utm_medium=organic`, `utm_campaign=04ho_content_factory`)가 전부 일치하는지 실제 생성 결과로 확인, 콘솔 에러 없음. git 커밋(`30aa55d`) 후 `git push origin main` + `vercel --prod` 배포 완료, 프로덕션(`https://04-sns-viral-feeder.vercel.app`)에서도 6카드·UTM 링크 3종 전부 재확인 완료, 콘솔 에러 없음(`No console logs.`)<br>2026-09-24 Make.com 웹훅 로그 도입(`app`/`event`/`timestamp`/`topic`/`status`/`errorMessage` 필드) 및 배포: 대장님이 전달한 `MAKE_LOG_WEBHOOK_URL`(Make.com 웹훅)을 `.env.local`+Vercel Production 환경변수에 등록. `app/api/generate-feed/route.ts`에 `logGenerationToMake()` 헬퍼 신규 — 매 생성 요청(성공/실패 공통)마다 웹훅으로 로그 전송, `try/catch`로 감싸 웹훅 실패가 본 기능(대본 생성)을 절대 막지 않도록 설계. 검증 중 Gemini `gemini-3.5-flash-lite`가 그 시점 일시적으로 과부하 상태(`503 UNAVAILABLE — high demand`)라 응답이 30~112초까지 걸리는 현상을 발견 — 코드/웹훅과 무관한 Gemini 쪽 일시적 지연으로 확인(잠시 후 12~23초대로 정상 회복). 웹훅 URL 자체를 독립적으로 직접 호출해 `200 Accepted` 확인, 로컬 3회 연속 생성에서 웹훅 전송 실패 로그 전무 확인 후 배포. 다만 Make.com 시나리오 실행 이력 자체는 대장님 계정이라 직접 열람은 못 했음을 보고서에 명시<br>2026-09-24 Make.com 로그에 실제 생성 원고 3개 필드(`thread_post`/`story_sticker_text`/`reels_pinned_comment`) 추가 및 배포: 기존 로그가 "언제/무슨 주제로/성공했는지"만 담고 실제 원고 내용이 안 남는 문제를 보완 — `logGenerationToMake()` payload에 성공(success) 케이스에서만 3개 필드를 채워 전송하도록 확장(실패 케이스는 해당 필드들이 `undefined`라 `JSON.stringify`가 자동으로 키 자체를 생략, 기존 `errorMessage` 필드는 그대로 유지). `reels_pinned_comment`는 서버가 매직링크(UTM 포함)를 이어붙인 최종 완성 텍스트를 그대로 로그에 담아, 실제로 사용자가 복사해 가는 내용과 로그가 정확히 일치하도록 함. 검증 방법: 로컬에 임시 mock HTTP 웹훅 서버(포트 3999)를 띄우고 `.env.local`의 `MAKE_LOG_WEBHOOK_URL`을 잠시 그쪽으로 돌려 실제 전송되는 raw payload를 직접 캡처 — 1차 캡처에서 `topic` 필드가 깨져 보이는 걸 발견했으나, 원인을 추적한 결과 curl 명령으로 직접 요청을 보낼 때의 인코딩 문제였을 뿐 애플리케이션 코드와는 무관함을 확인(mock 서버의 버퍼 조립 방식을 `Buffer.concat` 기반으로 고쳐 재확인했음에도 curl 경로에서만 재현되고, 실제 사용 경로인 브라우저 UI를 통한 재테스트에서는 `topic` 포함 8개 필드 전부 완벽하게 정상 표시됨을 확인). 테스트 후 mock 서버·캡처 파일 정리, `.env.local`을 실제 Make.com 웹훅 URL로 원복. `npm run build` 통과 후 git 커밋(`9b4362e`) → `git push origin main` → `vercel --prod` 배포 완료, 프로덕션(`https://04-sns-viral-feeder.vercel.app`)에서도 실제 생성 1건으로 6카드 정상 노출·콘솔 에러 없음(`No console logs.`) 재확인<br>2026-09-24 "원고 생성에 실패했습니다" 에러 긴급 진단 및 수정: 지시서가 제시한 3개 의심 원인(①MAKE_LOG_WEBHOOK_URL 등 Vercel 환경변수 누락 ②Gemini 일시 과부하 ③최근 웹훅 로그 필드 추가 커밋의 런타임 버그)을 순서대로 로그 우선 진단. `vercel env ls production`으로 `MAKE_LOG_WEBHOOK_URL`·`GEMINI_API_KEY` 둘 다 정상 등록 확인(①제외). `vercel logs` CLI 스트리밍은 상세 에러 바디를 누락해 로컬 dev 서버로 동일 `GEMINI_API_KEY`를 사용해 직접 재현 — 서버 콘솔에서 `Gemini 호출 실패 — non-OK response 503 {"error":{"code":503,"message":"This model is currently experiencing high demand...","status":"UNAVAILABLE"}}`를 그대로 확보, 연속 6회 테스트 중 5회가 503으로 확정 진단(③ 코드 버그 아님— 03호기가 이전에 겪은 것과 동일한 Gemini `gemini-3.5-flash-lite` 모델 자체의 외부 과부하 장애). 웹훅 로깅이 실패를 막는지도 재확인 — 실패 케이스에서도 "Make.com 로그 웹훅 전송 실패" 에러 없이 정상 흐름대로 502 응답까지 도달함을 로그로 재검증(②③ 결론: 순수 Gemini 외부 장애, 웹훅/최근 커밋과 무관). 지시서의 "재시도 로직 점검" 요청에 따라 `app/api/generate-feed/route.ts`에 짧은 재시도 로직 신규 추가 — 응답이 503일 때만(4xx 등 재시도해도 소용없는 에러는 즉시 반환) 1200ms 대기 후 최대 2회까지 추가 시도(총 3회), 재시도 횟수를 `errorMessage`(`gemini_non_ok_503_after_N_attempts`)와 성공 로그에도 함께 기록해 이후 진단이 쉽도록 함. 최종 실패 시 사용자 메시지도 "원고 생성에 실패했습니다. 잠시 후 다시 시도해주세요."로 보완. 로컬에서 재시도 전/후 비교 테스트 — 재시도 없을 때 6회 중 1회만 성공하던 것이 재시도 도입 후 5회 중 2회 즉시 성공+로그로 재시도 동작(503→1.2s 대기→재시도) 정상 확인. `npm run build` 통과 후 git 커밋(`99b1fbd`) → `git push origin main` → `vercel --prod` 배포 완료. 프로덕션에서 서로 다른 키워드 3개 연속 curl 테스트 전부 200 성공(38~50초 소요 — Gemini 자체가 여전히 느린 상태라 재시도 로직으로도 응답 시간 자체는 단축 못 하지만 성공률은 개선됨), 이어서 실제 브라우저 UI로도 별도 키워드 1건 재검증 — 6카드 정상 생성, 콘솔 에러 없음(`No console logs.`). 결론: 코드·환경변수·최근 커밋 전부 정상이었고, 원인은 100% Gemini 쪽 외부 서비스 일시 과부하였음 — 재시도 로직으로 체감 성공률을 끌어올렸으나 Gemini 장애가 심할 때는 여전히 실패할 수 있음을 사용자 메시지에도 반영<br>2026-09-25 스레드 원고 Solution 문단 광고 톤 제거 및 배포: 생성 결과의 Problem·Agitation 문단은 자연스러운 개인 서사인데 Solution 문단만 "이 도구 하나면 대본 걱정은 오늘부터 끝입니더" 식으로 제품 광고 카피처럼 이탈하던 문제를 프롬프트 레벨에서 수정. `app/api/generate-feed/route.ts`의 threadPost(1번 항목) Solution 규칙에 "도구/서비스/앱/이것/이거 하나면" 등 제품을 직접 지칭하는 표현을 금지하고, "그래서 답답한 마음에 뭘 어떻게 했더니 상황이 이렇게 편해졌다"는 순수 1인칭 후일담(행동+결과만 서술, 제품명·기능 설명 없음)으로만 쓰도록 명시 — 제품을 알리는 역할은 바로 뒤에 이어지는 "첫 댓글 링크" 카드가 담당하므로 본문은 그럴 필요가 없다는 점도 프롬프트에 근거로 포함. 마지막 대화 유도 질문도 "그래서 이게 필요하지 않으세요?"처럼 판매로 회귀하는 표현을 금지하고 순수 공감형 질문만 허용하도록 규칙 추가. 기존 Problem·Agitation 로직, 팩트 반영 빈도 제한, 첫 줄 질문형 금지 규칙은 변경하지 않았고, 지시서 제약대로 릴스 캡션(SEO 125자) 카드는 검색 노출 목적상 제품 설명이 필요해 이번 규칙 적용 대상에서 제외(실제로 캡션 카드에는 "AI 자동화 도구" 같은 표현이 계속 남아있음을 확인, 의도된 동작). `npm run build` 통과 후 로컬에서 동일 키워드("숏폼 대본 자동 생성", "퇴근 후 부업 추천") 2종으로 수정 전/후 비교 생성 — 수정 전 "밤마다 머리 싸맬 필요 없이 뚝딱 대본 뽑아주는 자동화 도구 하나 맹글었더만" 같은 제품 지칭 표현이, 수정 후 "그래서 답답한 마음에 퇴근하고 머리 싸매며 직접 하나 만들어봤는데, 이제는 밤에 멍 때릴 시간도 생기고 살 것 같다"처럼 제품 단어 없는 순수 경험담으로 명확히 바뀌었음을 확인, 마지막 질문도 두 사례 모두 순수 공감형으로 유지됨. git 커밋(`43c4a95`) → `git push origin main` → `vercel --prod` 배포 완료. 프로덕션(`https://04-sns-viral-feeder.vercel.app`)에서도 동일 키워드로 재검증 — Solution 문단 광고 톤 제거 재확인, 콘솔 에러 없음(`No console logs.`) |
